i have made an about us section. please add content about YCC there. (rn its 404 page not found).

after clicking on view details on either modal, the UI underlaps the newly placed logo, navigation bar links and a lot of below space is wasted so fix it.

in spec 01, i had specified that the college selection should be a dropdown menu, and the colleges inside are already given there. students should be able to select from those colleges only and the unique id should be generated from the same.

after the razorpay payment is successful, immediately the receipt should be downloaded automatically. the screen should warn students to wait until the receipt is downloaded. they are given an option to download the receipt later as well (with help of unique id or mobile number both). the QR should be such that it is easy to scan (so the QR code should be large enough.)
